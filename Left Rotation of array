#incude<bits/stdc++.h>
using namespace std;
void rotate(vector<int>&vec,int d)
{
 if(d==0)
 return;
 for(int i=0; i<d; i++)
 {
  vec.push_back(vec[0]);
  vec.erase(vec.begin());
  }
  for(inti=0; i<vec.size(); i++)
  {
   cout<<vec[i]<<"";
  }
 } 
